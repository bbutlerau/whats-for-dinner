"""Meal Planner — a weekly dinner calendar with pantry-aware colour coding."""

# The single source of truth for the version. pyproject.toml reads it from here
# via hatch, the footer displays it, and the release tag matches it. Bump it in
# the same commit as the CHANGELOG entry.
__version__ = "0.2.0"
